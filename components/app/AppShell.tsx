"use client";
/* eslint-disable react-hooks/purity, jsx-a11y/no-autofocus, jsx-a11y/anchor-is-valid, jsx-a11y/label-has-associated-control */

import { FormEvent, useEffect, useMemo, useState } from "react";
import Link from "next/link";
import {
  AlertTriangle, Award, BarChart3, Bell, Building2, CakeSlice, CalendarDays, Check, CheckCircle2, ChevronDown,
  CircleDollarSign, CreditCard, Gift, LayoutDashboard, Menu, MoreHorizontal,
  Plus, Search, Settings, ShieldCheck, Sparkles, Store, Users, WandSparkles, X, Zap,
} from "lucide-react";
import { format, formatDistanceToNow, isSameMonth } from "date-fns";
import { Logo } from "@/components/brand/Logo";
import { nextAnniversary, nextBirthday } from "@/lib/celebrations";
import type { Employee, Product, Workspace } from "@/lib/types";

type View = "dashboard" | "employees" | "celebrations" | "rewards" | "perkjoy-local" | "rules" | "reports" | "team" | "billing" | "settings";

const nav: { view: View; label: string; icon: typeof LayoutDashboard; href: string }[] = [
  { view: "dashboard", label: "Overview", icon: LayoutDashboard, href: "/dashboard" },
  { view: "employees", label: "Employees", icon: Users, href: "/employees" },
  { view: "celebrations", label: "Celebrations", icon: CalendarDays, href: "/celebrations" },
  { view: "rewards", label: "Rewards", icon: Gift, href: "/rewards" },
  { view: "perkjoy-local", label: "PerkJoy Local", icon: Store, href: "/perkjoy-local" },
  { view: "rules", label: "Automation Rules", icon: Zap, href: "/rules" },
  { view: "reports", label: "Reports", icon: BarChart3, href: "/reports" },
  { view: "team", label: "Team", icon: Building2, href: "/team" },
  { view: "billing", label: "Billing", icon: CreditCard, href: "/billing" },
  { view: "settings", label: "Settings", icon: Settings, href: "/settings" },
];

type Celebration = { employee: Employee; type: "Birthday" | "Anniversary"; date: Date; label: string; amount: number; tone: string };

function money(cents: number) { return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(cents / 100); }
function initials(employee: Employee) { return `${employee.firstName[0]}${employee.lastName[0]}`; }
function fullName(employee?: Employee) { return employee ? `${employee.firstName} ${employee.lastName}` : "Unknown employee"; }

export function AppShell({ view = "dashboard" }: { view?: View }) {
  const [data, setData] = useState<Workspace | null>(null);
  const [error, setError] = useState("");
  const [busy, setBusy] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);
  const [modal, setModal] = useState<"employee" | "recognize" | "quick" | "concierge" | "order" | null>(null);
  const [selectedEmployee, setSelectedEmployee] = useState<string>("");
  const [selectedProduct, setSelectedProduct] = useState<Product | null>(null);

  async function load() {
    try {
      const response = await fetch("/api/workspace", { cache: "no-store" });
      const json = await response.json() as Workspace & { error?: string };
      if (!response.ok) throw new Error(json.error || "Unable to load workspace");
      setData(json); setError("");
    } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to load workspace"); }
  }

  async function mutate(payload: Record<string, unknown>) {
    setBusy(true); setError("");
    try {
      const response = await fetch("/api/workspace", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) });
      const json = await response.json() as Workspace & { error?: string };
      if (!response.ok) throw new Error(json.error || "Unable to save change");
      setData(json); setModal(null);
    } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to save change"); }
    finally { setBusy(false); }
  }

  useEffect(() => {
    let active = true;
    fetch("/api/workspace", { cache: "no-store" })
      .then(async (response) => {
        const json = await response.json() as Workspace & { error?: string };
        if (!response.ok) throw new Error(json.error || "Unable to load workspace");
        if (active) { setData(json); setError(""); }
      })
      .catch((reason: unknown) => { if (active) setError(reason instanceof Error ? reason.message : "Unable to load workspace"); });
    return () => { active = false; };
  }, []);

  const celebrations = useMemo<Celebration[]>(() => {
    if (!data) return [];
    return data.employees.flatMap((employee, index) => {
      const birthday = nextBirthday(employee);
      const anniversary = nextAnniversary(employee);
      return [
        { employee, type: "Birthday" as const, date: birthday, label: "Birthday", amount: 5000, tone: ["coral", "purple", "green"][index % 3] },
        { employee, type: "Anniversary" as const, date: anniversary.date, label: `${anniversary.years} Year Anniversary`, amount: 7500, tone: "gold" },
      ];
    }).sort((a, b) => a.date.getTime() - b.date.getTime()).slice(0, 10);
  }, [data]);

  if (!data && !error) return <AppSkeleton />;

  return (
    <div className="app-page">
      <aside className={`app-sidebar ${mobileOpen ? "open" : ""}`}>
        <div className="sidebar-head"><Link href="/"><Logo /></Link><button className="mobile-close" onClick={() => setMobileOpen(false)} aria-label="Close navigation"><X /></button></div>
        <nav className="app-nav">
          <small>WORKSPACE</small>
          {nav.slice(0, 7).map((item) => <Link className={item.view === view ? "active" : ""} href={item.href} key={item.view} onClick={() => setMobileOpen(false)}><item.icon />{item.label}{item.view === "celebrations" && <em>8</em>}</Link>)}
          <small>MANAGE</small>
          {nav.slice(7).map((item) => <Link className={item.view === view ? "active" : ""} href={item.href} key={item.view} onClick={() => setMobileOpen(false)}><item.icon />{item.label}</Link>)}
        </nav>
        <div className="sidebar-card"><span><Sparkles /></span><b>Moments handled</b><p>12 celebrations handled this quarter.</p><div><i style={{ width: "78%" }} /></div></div>
        <div className="sidebar-user"><span>TM</span><div><b>Taylor Morgan</b><small>Owner · Philly Creative</small></div><MoreHorizontal /></div>
      </aside>

      {mobileOpen && <button className="sidebar-overlay" onClick={() => setMobileOpen(false)} aria-label="Close navigation" />}

      <section className="app-workspace">
        <header className="app-topbar"><button className="mobile-menu" onClick={() => setMobileOpen(true)} aria-label="Open navigation"><Menu /></button><div className="company-switcher"><span>PC</span><b>{data?.organization.name}</b><ChevronDown /></div><div className="topbar-actions"><button aria-label="Search"><Search /></button><button className="notification-button" aria-label="Notifications"><Bell /><i /></button><button className="button button-primary recognize-top" onClick={() => { setSelectedEmployee(data?.employees[0]?.id ?? ""); setModal("recognize"); }}><Plus /> Celebrate Someone</button></div></header>
        <main className="app-content">
          {error && <div className="app-error"><span>{error}</span><button onClick={load}>Try again</button></div>}
          {data && <ViewContent view={view} data={data} celebrations={celebrations} mutate={mutate} openEmployee={() => setModal("employee")} openRecognize={(id) => { setSelectedEmployee(id ?? data.employees[0]?.id ?? ""); setModal("recognize"); }} openQuick={(id) => { setSelectedEmployee(id ?? data.employees[0]?.id ?? ""); setModal("quick"); }} openConcierge={(id) => { setSelectedEmployee(id ?? data.employees[0]?.id ?? ""); setModal("concierge"); }} openOrder={(product) => { setSelectedProduct(product); setSelectedEmployee(data.employees[0]?.id ?? ""); setModal("order"); }} />}
        </main>
      </section>

      <nav className="mobile-bottom-nav" aria-label="Mobile app navigation"><Link href="/dashboard"><LayoutDashboard /><span>Dashboard</span></Link><Link href="/celebrations"><CalendarDays /><span>Calendar</span></Link><button onClick={() => { setSelectedEmployee(data?.employees[0]?.id ?? ""); setModal("recognize"); }}><Plus /><span>Celebrate</span></button><Link href="/employees"><Users /><span>Employees</span></Link><button onClick={() => setMobileOpen(true)}><Menu /><span>More</span></button></nav>

      {modal === "employee" && <AddEmployeeModal busy={busy} close={() => setModal(null)} submit={mutate} />}
      {modal === "recognize" && data && <RecognitionModal employees={data.employees} selected={selectedEmployee} setSelected={setSelectedEmployee} busy={busy} close={() => setModal(null)} submit={mutate} />}
      {modal === "quick" && data && <QuickCelebrateModal data={data} selected={selectedEmployee} setSelected={setSelectedEmployee} busy={busy} close={() => setModal(null)} submit={mutate} />}
      {modal === "concierge" && data && <ConciergeModal employees={data.employees} selected={selectedEmployee} setSelected={setSelectedEmployee} busy={busy} close={() => setModal(null)} submit={mutate} />}
      {modal === "order" && data && selectedProduct && <OrderModal employees={data.employees} product={selectedProduct} selected={selectedEmployee} setSelected={setSelectedEmployee} busy={busy} close={() => setModal(null)} submit={mutate} />}
    </div>
  );
}

function ViewContent({ view, data, celebrations, mutate, openEmployee, openRecognize, openQuick, openConcierge, openOrder }: { view: View; data: Workspace; celebrations: Celebration[]; mutate: (payload: Record<string, unknown>) => void; openEmployee: () => void; openRecognize: (id?: string) => void; openQuick: (id?: string) => void; openConcierge: (id?: string) => void; openOrder: (product: Product) => void }) {
  if (view === "dashboard") return <Dashboard data={data} mutate={mutate} openQuick={openQuick} />;
  if (view === "employees") return <EmployeesView data={data} celebrations={celebrations} openEmployee={openEmployee} openRecognize={openRecognize} mutate={mutate} />;
  if (view === "celebrations") return <CelebrationsView data={data} mutate={mutate} />;
  if (view === "rewards") return <RewardsView data={data} openRecognize={openRecognize} />;
  if (view === "perkjoy-local") return <LocalView data={data} openOrder={openOrder} openConcierge={openConcierge} />;
  if (view === "rules") return <RulesView data={data} mutate={mutate} />;
  if (view === "reports") return <ReportsView data={data} />;
  if (view === "team") return <TeamView />;
  if (view === "billing") return <BillingView />;
  return <SettingsView data={data} mutate={mutate} />;
}

function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description: string; action?: React.ReactNode }) {
  return <div className="app-page-head"><div>{eyebrow && <small>{eyebrow}</small>}<h1>{title}</h1><p>{description}</p></div>{action}</div>;
}

function eventStatusLabel(status: string) {
  return ({ needs_attention: "Needs Attention", scheduled: "Scheduled", approval_required: "Approval Required", handled: "Handled", delivered: "Delivered", skipped: "Skipped" } as Record<string, string>)[status] ?? status;
}

function Dashboard({ data, mutate, openQuick }: { data: Workspace; mutate: (payload: Record<string, unknown>) => void; openQuick: (id?: string) => void }) {
  const [today] = useState(() => new Date());
  const monthRewards = data.rewards.filter((reward) => isSameMonth(new Date(reward.createdAt), today));
  const spend = monthRewards.reduce((sum, reward) => sum + reward.amountCents, 0) + data.localOrders.reduce((sum, order) => sum + order.totalCents, 0);
  const handled = data.events.filter((event) => ["scheduled", "handled", "delivered"].includes(event.status));
  const attention = data.events.filter((event) => event.status === "needs_attention");
  const pendingApprovals = data.approvals.filter((approval) => approval.status === "pending");
  const health = data.events.length ? Math.round(handled.length / data.events.length * 100) : 100;
  const quickRecommendation = data.recommendations.find((recommendation) => recommendation.status === "recommended") ?? data.recommendations[0];
  return <>
    <PageHeader eyebrow="CELEBRATION OPERATING SYSTEM" title="PerkJoy has this handled." description="You hire the people. PerkJoy remembers the moments." action={<div className="header-date"><CalendarDays /><span><small>Today</small><b>{format(today, "EEEE, MMMM d")}</b></span></div>} />
    <section className="handled-hero app-card"><div><span><CheckCircle2 /></span><div><small>MOMENTS HANDLED THIS MONTH</small><h2>{handled.length} moments handled</h2><p>{handled.length} of {data.events.length} upcoming celebrations are already on track.</p></div></div><div className="health-score"><span style={{ "--health": `${health * 3.6}deg` } as React.CSSProperties}><b>{health}%</b></span><div><small>CELEBRATION HEALTH</small><b>{health >= 90 ? "Excellent" : health >= 70 ? "Good" : "Needs Attention"}</b><p>Are we taking care of our people? Yes—with {attention.length} next step{attention.length === 1 ? "" : "s"}.</p></div></div></section>
    <div className="dashboard-priority-grid"><section className="app-card moment-calendar"><div className="card-head"><div><h2>What&apos;s Coming Up</h2><p>Every moment, reward, and status in one place.</p></div><Link href="/celebrations">Open calendar →</Link></div><div className="moment-list">{data.events.slice(0, 5).map((event) => { const employee = data.employees.find((item) => item.id === event.employeeId); const date = new Date(`${event.eventDate}T12:00:00`); return <article key={event.id}><div className="moment-when"><b>{formatDistanceToNow(date, { addSuffix: false })}</b><small>{format(date, "MMM d")}</small></div><span className="avatar avatar-coral">{employee ? initials(employee) : "?"}</span><div className="moment-name"><b>{event.title}</b><small>{event.category === "life" ? "Life event" : "Career event"}</small></div><div className="moment-reward"><b>{event.rewardSummary}</b><small>{event.status === "needs_attention" ? "PerkJoy needs one choice" : "PerkJoy is tracking every step"}</small></div><em className={`moment-status ${event.status}`}>{eventStatusLabel(event.status)}</em>{event.status === "needs_attention" && <button className="button button-primary button-small" onClick={() => mutate({ action: "handleEvent", eventId: event.id })}>Handle This</button>}</article>; })}</div></section><aside className="attention-stack"><section className="app-card attention-card"><div className="card-head"><div><h2>Needs Your Attention</h2><p>Only the things that need a human.</p></div><span>{attention.length + pendingApprovals.length}</span></div>{attention.map((event) => <article key={event.id}><AlertTriangle /><div><b>{event.title} is coming up.</b><p>No celebration is scheduled.</p></div><button onClick={() => mutate({ action: "handleEvent", eventId: event.id })}>Handle This</button></article>)}{pendingApprovals.map((approval) => { const recommendation = data.recommendations.find((item) => item.id === approval.entityId); return <article key={approval.id}><ShieldCheck /><div><b>{recommendation?.title ?? "A reward"} needs approval.</b><p>{money(approval.amountCents)} · {approval.approvalLevel} approval</p></div><button onClick={() => mutate({ action: "approveRequest", approvalId: approval.id })}>Approve</button></article>; })}{!attention.length && !pendingApprovals.length && <div className="all-handled"><Check /><b>You&apos;re all caught up.</b><p>We&apos;ll let you know when the next moment needs you.</p></div>}</section><section className="app-card quick-celebrate-card"><div><span><WandSparkles /></span><small>QUICK CELEBRATE</small><h2>{quickRecommendation ? `Celebrate ${data.employees.find((employee) => employee.id === quickRecommendation.employeeId)?.firstName}` : "Celebrate someone"}</h2><p>{quickRecommendation ? quickRecommendation.title : "Pick an employee and PerkJoy will recommend the reward."}</p></div>{quickRecommendation && <div className="recommendation-reason"><b>Why this?</b><p>{quickRecommendation.recommendationReason}</p>{quickRecommendation.somethingDifferent && <em>Something different this year</em>}</div>}<button className="button button-dark" onClick={() => openQuick(quickRecommendation?.employeeId)}>Quick Celebrate</button></section></aside></div>
    <section className="app-card handled-detail"><div className="card-head"><div><h2>PerkJoy Has It Handled</h2><p>Confidence without the follow-up.</p></div><span><Sparkles /> {handled.length} celebrations on track</span></div>{data.events.filter((event) => event.handledSteps !== "[]").slice(0, 3).map((event) => <article key={event.id}><div><b>{event.title}</b><small>{format(new Date(`${event.eventDate}T12:00:00`), "MMMM d")}</small></div><div>{(JSON.parse(event.handledSteps) as string[]).map((step, index, steps) => <span key={step}>{index === steps.length - 1 && event.status === "scheduled" ? <i className="pending-step" /> : <Check />} {step}</span>)}</div><em className={`moment-status ${event.status}`}>{eventStatusLabel(event.status)}</em></article>)}</section>
    <div className="dashboard-bottom-grid"><ActivityCard data={data} /><BudgetCard data={data} spend={spend} /></div>
  </>;
}

function BudgetCard({ data, spend }: { data: Workspace; spend: number }) { const pct = Math.min(100, Math.round(spend / data.organization.monthlyBudgetCents * 100)); return <section className="app-card budget-card"><div className="card-head"><div><h2>Monthly budget</h2><p>August recognition spend</p></div><button><MoreHorizontal /></button></div><div className="budget-ring" style={{ "--progress": `${pct * 3.6}deg` } as React.CSSProperties}><div><small>Remaining</small><b>{money(Math.max(0, data.organization.monthlyBudgetCents - spend))}</b></div></div><div className="budget-detail"><span><i className="committed" />Committed<b>{money(spend)}</b></span><span><i className="remaining" />Budget<b>{money(data.organization.monthlyBudgetCents)}</b></span></div><Link href="/settings">Manage budget <span>→</span></Link></section>; }

function ActivityCard({ data }: { data: Workspace }) { const latest = data.rewards.slice(0, 3); return <section className="app-card activity-card"><div className="card-head"><div><h2>Recent activity</h2><p>Recognition across your team</p></div></div><div>{latest.map((reward) => { const employee = data.employees.find((item) => item.id === reward.employeeId); return <span key={reward.id}><i className="activity-icon"><Award /></i><p><b>{fullName(employee)}</b> was recognized for {reward.recognitionType}<small>{formatDistanceToNow(new Date(reward.createdAt), { addSuffix: true })}</small></p><em>{money(reward.amountCents)}</em></span>; })}</div><Link href="/rewards">View all activity →</Link></section>; }

function EmployeesView({ data, celebrations, openEmployee, openRecognize, mutate }: { data: Workspace; celebrations: Celebration[]; openEmployee: () => void; openRecognize: (id?: string) => void; mutate: (payload: Record<string, unknown>) => void }) {
  const [query, setQuery] = useState("");
  const filtered = data.employees.filter((e) => fullName(e).toLowerCase().includes(query.toLowerCase()) || e.department.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeader eyebrow="PEOPLE" title="Employees" description="Manage your team, their moments, and private Celebration Profile invitations." action={<div className="head-actions"><button className="button button-secondary">Import CSV</button><button className="button button-primary" onClick={openEmployee}><Plus /> Add employee</button></div>} /><div className="profile-privacy-banner"><ShieldCheck /><div><b>Private by design</b><p>Private preferences and dietary selections are used for recommendations but never shown to unauthorized managers.</p></div></div><div className="toolbar"><label><Search /><input placeholder="Search employees" value={query} onChange={(e) => setQuery(e.target.value)} /></label><select aria-label="Department"><option>All departments</option><option>Design</option><option>Engineering</option></select><select aria-label="Status"><option>Active employees</option></select><span>{filtered.length} people</span></div><section className="app-card data-table profile-table"><div className="table-row table-head"><span>Employee</span><span>Department</span><span>Work mode</span><span>Next moment</span><span>Celebration Profile</span><span>Status</span><span /></div>{filtered.map((employee) => { const next = data.events.find((event) => event.employeeId === employee.id); const profile = data.profiles.find((item) => item.employeeId === employee.id); return <div className="table-row" key={employee.id}><span className="employee-cell"><i className="avatar avatar-coral">{initials(employee)}</i><p><b>{fullName(employee)}</b><small>{employee.email}</small></p></span><span><b>{employee.department}</b><small>{employee.jobTitle}</small></span><span><b className="capitalize">{profile?.workMode ?? "office"}</b><small>{profile?.preferredDelivery.replace("_", " ")}</small></span><span><b>{next?.title ?? celebrations.find((c) => c.employee.id === employee.id)?.label}</b><small>{next ? format(new Date(`${next.eventDate}T12:00:00`), "MMM d") : "Watching calendar"}</small></span><span><div className="profile-completeness"><b>{profile?.completeness ?? 0}%</b><i><span style={{ width: `${profile?.completeness ?? 0}%` }} /></i></div><button onClick={() => mutate({ action: "refreshProfileInvite", employeeId: employee.id })}>Create profile link</button></span><span><em className="status-badge active"><i /> Active</em></span><span><button className="icon-button" onClick={() => openRecognize(employee.id)} aria-label={`Celebrate ${fullName(employee)}`}><Award /></button></span></div>; })}</section><div className="profile-invite-help"><div><Sparkles /><span><b>Create Your Celebration Profile</b><small>Employees receive a secure, expiring link. They do not need a company account.</small></span></div>{data.profiles[0] && <Link className="button button-secondary" href={`/celebrate/${data.profiles[0].inviteToken}`}>Preview employee form</Link>}</div></>;
}

function CelebrationsView({ data, mutate }: { data: Workspace; mutate: (payload: Record<string, unknown>) => void }) { const career = data.celebrationTypes.filter((type) => type.category === "career"); const life = data.celebrationTypes.filter((type) => type.category === "life"); return <><PageHeader eyebrow="CELEBRATION CALENDAR" title="What&apos;s Coming Up" description="Every employee, event, reward, and status—at a glance." action={<div className="segmented"><button className="active">Upcoming</button><button>Month</button></div>} /><div className="calendar-summary"><span><CakeSlice /> <b>{data.events.filter((event) => event.category === "life").length}</b> life events</span><span><Award /> <b>{data.events.filter((event) => event.category === "career").length}</b> career events</span><span><CheckCircle2 /> <b>{data.events.filter((event) => ["scheduled","handled","delivered"].includes(event.status)).length}</b> handled</span></div><section className="app-card enhanced-timeline">{data.events.map((event, index) => { const employee = data.employees.find((item) => item.id === event.employeeId); const steps = JSON.parse(event.handledSteps) as string[]; return <article key={event.id}><span className="timeline-line">{index !== data.events.length - 1 && <i />}</span><div className={`date-block ${event.category === "life" ? "coral" : "gold"}`}><small>{format(new Date(`${event.eventDate}T12:00:00`), "MMM")}</small><b>{format(new Date(`${event.eventDate}T12:00:00`), "dd")}</b></div><span className="avatar avatar-green">{employee ? initials(employee) : "?"}</span><div className="calendar-event-detail"><b>{event.title}</b><p>{employee?.department} · {event.rewardSummary}</p>{steps.length > 0 && <small>{steps.map((step) => `✓ ${step}`).join("  ·  ")}</small>}</div><em>{formatDistanceToNow(new Date(`${event.eventDate}T12:00:00`), { addSuffix: true })}</em><span className={`moment-status ${event.status}`}>{eventStatusLabel(event.status)}</span>{event.status === "needs_attention" && <button className="button button-primary button-small" onClick={() => mutate({ action: "handleEvent", eventId: event.id })}>Handle This</button>}</article>; })}</section><section className="app-card celebration-type-manager"><div className="card-head"><div><h2>What should PerkJoy remember?</h2><p>Enable the celebration types that fit your company. Life events are added manually or volunteered by employees.</p></div></div><div><fieldset><legend>Career events</legend>{career.map((type) => <label key={type.id}><span><b>{type.name}</b><small>{type.manualOnly ? "Added by managers" : "Can be automated"}</small></span><button className={`switch ${type.active ? "on" : ""}`} onClick={() => mutate({ action: "toggleCelebrationType", typeId: type.id })} aria-label={`Toggle ${type.name}`}><i /></button></label>)}</fieldset><fieldset><legend>Life events</legend>{life.map((type) => <label key={type.id}><span><b>{type.name}</b><small>{type.manualOnly ? "Voluntary or manually added" : "Can be automated"}</small></span><button className={`switch ${type.active ? "on" : ""}`} onClick={() => mutate({ action: "toggleCelebrationType", typeId: type.id })} aria-label={`Toggle ${type.name}`}><i /></button></label>)}</fieldset></div></section></>; }

function RewardsView({ data, openRecognize }: { data: Workspace; openRecognize: (id?: string) => void }) { return <><PageHeader eyebrow="RECOGNITION" title="Rewards" description="Track every thank-you, celebration, and delivery." action={<button className="button button-primary" onClick={() => openRecognize()}><Plus /> Send recognition</button>} /><div className="reward-stats"><article><Gift /><div><small>Total sent</small><b>{data.rewards.length}</b></div></article><article><CircleDollarSign /><div><small>Reward volume</small><b>{money(data.rewards.reduce((s, r) => s + r.amountCents, 0))}</b></div></article><article><Check /><div><small>Delivery rate</small><b>100%</b></div></article></div><section className="app-card data-table rewards-table"><div className="table-row table-head"><span>Recipient</span><span>Recognition</span><span>Reward</span><span>Provider</span><span>Date</span><span>Status</span></div>{data.rewards.map((reward) => { const employee = data.employees.find((e) => e.id === reward.employeeId); return <div className="table-row" key={reward.id}><span className="employee-cell"><i className="avatar avatar-coral">{employee ? initials(employee) : "?"}</i><p><b>{fullName(employee)}</b><small>{employee?.email}</small></p></span><span><b>{reward.recognitionType}</b><small>{reward.message}</small></span><span><b>{money(reward.amountCents)}</b></span><span>{reward.provider.replace("_", " ")}</span><span>{format(new Date(reward.createdAt), "MMM d, yyyy")}</span><span><em className="status-badge active"><i /> {reward.status}</em></span></div>; })}</section><div className="sandbox-note"><ShieldCheck /> <div><b>Safe development mode is on</b><p>Digital rewards use Tremendous Sandbox and are clearly marked as TEST. Production endpoints are blocked.</p></div></div></>; }

function LocalView({ data, openOrder, openConcierge }: { data: Workspace; openOrder: (product: Product) => void; openConcierge: (id?: string) => void }) { const [category, setCategory] = useState("All"); const categories = ["All", "Cakes & Treats", "Food & Lunch", "Flowers", "Celebration", "Gift Boxes", "Coffee", "Experiences"]; const filtered = category === "All" ? data.products : data.products.filter((product) => product.category === category); return <><div className="local-app-hero local-core-hero"><div><span><MapPinSmall /> Now launching in Philadelphia</span><h1>Turn employee appreciation<br />into a real moment.</h1><p>Cakes, flowers, lunches, gifts, and experiences from businesses in your city—with availability and delivery handled by PerkJoy.</p><div><button className="button button-light" onClick={() => openConcierge()}>Plan with Concierge</button><small>More cities coming soon</small></div></div><div className="local-app-art"><CakeSlice /><span>Made in Philly</span></div></div><section className="market-switcher">{data.markets.map((market) => <span className={market.active ? "active" : ""} key={market.id}><b>{market.name}</b><small>{market.active ? "Active" : "Coming soon"}</small></span>)}</section><div className="marketplace-filters"><div className="local-categories">{categories.map((item) => <button className={category === item ? "active" : ""} onClick={() => setCategory(item)} key={item}>{item}</button>)}</div><div><select aria-label="Occasion"><option>Any occasion</option><option>Birthday</option><option>Team achievement</option><option>New hire</option></select><select aria-label="Budget"><option>Any budget</option><option>Under $50</option><option>$50–$100</option><option>$100+</option></select><input type="date" aria-label="Delivery date" /><label><input type="checkbox" /> Preference match</label></div></div><div className="catalog-grid enhanced-catalog">{filtered.map((product, index) => <article key={product.id}><div className={`catalog-art art-${index % 3 + 1}`}><span>{product.category === "Flowers" ? "💐" : product.category === "Food & Lunch" ? "🍽️" : product.category === "Coffee" ? "☕" : product.category === "Gift Boxes" ? "🎁" : index % 2 ? "🧁" : "🎂"}</span><em>Demo vendor</em></div><div><small>{product.category}</small><h3>{product.name}</h3><p>{product.description}</p><div className="catalog-meta"><span>Delivery available</span><span>48–72 hr lead time</span><span>★ 4.9</span></div><span><b>{money(product.priceCents)}</b><small> + {money(product.deliveryFeeCents)} delivery</small></span><button className="button button-primary" onClick={() => openOrder(product)}>Send this gift</button></div></article>)}</div><section className="bundle-section"><div className="card-head"><div><small>PERKJOY BUNDLES</small><h2>A whole celebration, already planned.</h2><p>One vendor, one approval, every thoughtful detail.</p></div></div><div className="bundle-grid">{data.bundles.map((bundle) => <article className="app-card" key={bundle.id}><span>{bundle.category === "Team" ? "🥳" : bundle.category === "Achievement" ? "🏆" : "🎂"}</span><small>{bundle.vendorName}</small><h3>{bundle.name}</h3><p>{bundle.description}</p><ul>{bundle.items.slice(0, 4).map((item) => <li key={item.id}><Check /> {item.quantity > 1 ? `${item.quantity} ` : ""}{item.name}</li>)}</ul><footer><b>{money(bundle.customerPriceCents)}{bundle.customerPriceCents >= 14900 ? "+" : ""}</b><button className="button button-secondary">Choose bundle</button></footer></article>)}</div></section><section className="concierge-banner"><div><WandSparkles /><span><small>PERKJOY CONCIERGE</small><h2>Tell us the occasion and budget. We&apos;ll handle the rest.</h2><p>PerkJoy uses employee preferences to plan a celebration, then asks for your approval before anything is purchased.</p></span></div><button className="button button-light" onClick={() => openConcierge()}>Start a Concierge Request</button></section>{data.localOrders.length > 0 && <section className="app-card order-status"><div className="card-head"><div><h2>Local gift orders</h2><p>Availability, fulfillment, and delivery in one queue.</p></div></div>{data.localOrders.map((order) => <div key={order.id}><span className="activity-icon"><CakeSlice /></span><p><b>{data.products.find((p) => p.id === order.productId)?.name}</b><small>For {fullName(data.employees.find((e) => e.id === order.employeeId))} · Delivery {format(new Date(`${order.deliveryDate}T12:00:00`), "MMM d")}</small></p><em className="status-badge active">{order.status === "delivered" ? "Delivered! 🎂" : order.status}</em><b>{money(order.totalCents)}</b></div>)}</section>}</>; }

function MapPinSmall() { return <Store size={15} />; }

function RulesView({ data, mutate }: { data: Workspace; mutate: (payload: Record<string, unknown>) => void }) { return <><PageHeader eyebrow="AUTOMATION" title="Celebration rules" description="Set it once. PerkJoy keeps every moment on track." action={<button className="button button-primary"><Plus /> Create rule</button>} /><div className="mode-banner"><div><span><Zap /></span><div><small>APPROVAL MODE</small><b>Automatic with safeguards</b><p>Eligible rewards send automatically. Anniversary rewards over $50 require approval.</p></div></div><button>Manage mode</button></div><section className="rules-grid">{data.rules.map((rule) => <article className="app-card rule-card" key={rule.id}><div><span className={`rule-icon ${rule.active ? "active" : "paused"}`}>{rule.eventType === "Birthday" ? <CakeSlice /> : rule.eventType === "Anniversary" ? <Award /> : <Sparkles />}</span><button className="icon-button"><MoreHorizontal /></button></div><small>{rule.eventType.toUpperCase()}</small><h3>{rule.name}</h3><div className="rule-flow"><span>{rule.eventType}</span><i>→</i><span>{rule.rewardType} · {money(rule.amountCents)}</span></div><p><CalendarDays /> {rule.timing}</p><footer><span className={`status-badge ${rule.active ? "active" : "paused"}`}><i /> {rule.active ? "Active" : "Paused"}</span><button className={`switch ${rule.active ? "on" : ""}`} onClick={() => mutate({ action: "toggleRule", ruleId: rule.id })} aria-label={`Toggle ${rule.name}`}><i /></button></footer></article>)}</section><div className="empty-add-card"><span><Plus /></span><div><b>Build another celebration rule</b><p>Automate promotions, new-hire welcomes, custom milestones, and more.</p></div><button className="button button-secondary">Create rule</button></div></>; }

function ReportsView({ data }: { data: Workspace }) { const digital = data.rewards.reduce((s, r) => s + r.amountCents, 0); const local = data.localOrders.reduce((s, o) => s + o.totalCents, 0); const total = digital + local; const handled = data.events.filter((event) => ["scheduled","handled","delivered"].includes(event.status)).length; const health = data.events.length ? Math.round(handled / data.events.length * 100) : 100; return <><PageHeader eyebrow="CELEBRATION ANALYTICS" title="Are we taking care of our people?" description="Measure whether moments are remembered, personalized, and delivered—not vanity activity." action={<select className="date-select"><option>This month</option><option>Last 90 days</option></select>} /><div className="report-kpis report-kpis-four"><article><small>Moments Handled</small><b>{handled}</b><em>{health}% of upcoming moments</em></article><article><small>Employees Celebrated</small><b>{new Set([...data.rewards.map((r) => r.employeeId), ...data.events.filter((event) => event.status === "delivered").map((event) => event.employeeId)]).size}</b><em>of {data.employees.length} employees</em></article><article><small>Local Gifts Delivered</small><b>{data.localOrders.filter((order) => order.status === "delivered").length}</b><em>{data.localOrders.length} local orders total</em></article><article><small>Average Celebration Spend</small><b>{money((data.rewards.length + data.localOrders.length) ? total / (data.rewards.length + data.localOrders.length) : 0)}</b><em>Digital + local</em></article></div><div className="report-feature-grid"><section className="app-card celebration-health-report"><div className="health-report-ring" style={{ "--health": `${health * 3.6}deg` } as React.CSSProperties}><span><b>{health}%</b><small>Good</small></span></div><div><small>CELEBRATION HEALTH</small><h2>{health >= 90 ? "Excellent" : health >= 70 ? "Good" : "Needs Attention"}</h2><p>Based on rewards configured, approvals completed, delivery information, and successful scheduling.</p><ul><li><Check /> {handled} moments ready or delivered</li><li><AlertTriangle /> {data.events.length - handled} moment needs attention</li></ul></div></section><section className="app-card spend-breakdown"><div className="card-head"><div><h2>Celebration spend</h2><p>Reward value stays separate from subscription revenue.</p></div></div><div><span><i style={{ width: `${total ? digital / total * 100 : 0}%` }} /></span><p><b>Digital spend</b><em>{money(digital)}</em></p><p><b>Local spend</b><em>{money(local)}</em></p><p><b>Total celebration spend</b><em>{money(total)}</em></p></div></section></div><section className="app-card report-chart"><div className="card-head"><div><h2>Moments handled by type</h2><p>Real celebrations completed across your company.</p></div></div><div className="bar-chart">{[["Birthdays",88,"#f27a5f"],["Anniversaries",72,"#e6b94d"],["Manager recognition",64,"#548c78"],["Local gifts",48,"#8166aa"]].map(([label, value, color]) => <span key={String(label)}><small>{label}</small><i><b style={{ width: `${value}%`, background: String(color) }} /></i><em>{value}%</em></span>)}</div></section></>; }

function TeamView() { return <><PageHeader eyebrow="ACCESS" title="Your PerkJoy team" description="Invite teammates and control what they can see and do." action={<button className="button button-primary"><Plus /> Invite teammate</button>} /><section className="app-card data-table team-table"><div className="table-row table-head"><span>Team member</span><span>Role</span><span>Access</span><span>Status</span><span /></div>{[["Taylor Morgan","taylor@phillycreative.demo","Owner","Full organization"],["Jordan Lee","jordan@phillycreative.demo","Manager","Design team"],["Priya Shah","priya@phillycreative.demo","Viewer","Reports only"]].map(([name,email,role,access], index) => <div className="table-row" key={email}><span className="employee-cell"><i className={`avatar ${index === 1 ? "avatar-green" : "avatar-coral"}`}>{name.split(" ").map((p) => p[0]).join("")}</i><p><b>{name}</b><small>{email}</small></p></span><span><b>{role}</b></span><span>{access}</span><span><em className="status-badge active"><i /> Active</em></span><span><button className="icon-button"><MoreHorizontal /></button></span></div>)}</section></>; }

function BillingView() { return <><PageHeader eyebrow="PLAN & BILLING" title="Billing" description="Manage your PerkJoy subscription and payment details." /><div className="billing-grid"><section className="app-card plan-card"><span>GROWTH PLAN</span><h2>$79<small>/month</small></h2><p>For teams with up to 50 employees.</p><div><Check /> Automated celebrations</div><div><Check /> PerkJoy Local access</div><div><Check /> Reports and budget controls</div><button className="button button-primary">Manage subscription</button></section><section className="app-card payment-card"><div className="card-head"><div><h2>Payment method</h2><p>Used for your SaaS subscription only.</p></div></div><div className="credit-card-row"><span><CreditCard /></span><p><b>Visa ending in 4242</b><small>Expires 08/29</small></p><button>Edit</button></div><div className="billing-note"><ShieldCheck /> Reward purchases and your PerkJoy subscription are billed separately.</div></section></div></>; }

function SettingsView({ data, mutate }: { data: Workspace; mutate: (payload: Record<string, unknown>) => void }) { const [budget, setBudget] = useState(String(data.organization.monthlyBudgetCents / 100)); return <><PageHeader eyebrow="ORGANIZATION" title="Settings" description="Configure how PerkJoy works for your company." /><div className="settings-layout"><aside><a className="active">Company profile</a><a>Recognition settings</a><a>Notifications</a><a>Security</a></aside><section className="app-card settings-card"><div><h2>Company profile</h2><p>Your company details and recognition preferences.</p></div><label>Company name<input defaultValue={data.organization.name} /></label><div className="form-grid"><label>Timezone<select defaultValue={data.organization.timezone}><option>America/New_York</option><option>America/Chicago</option><option>America/Los_Angeles</option></select></label><label>Approval mode<select defaultValue="automatic"><option value="automatic">Automatic with safeguards</option><option>Approval required</option><option>Reminder only</option></select></label></div><div className="form-grid"><label>Monthly recognition budget<div className="money-input"><span>$</span><input type="number" value={budget} onChange={(e) => setBudget(e.target.value)} /></div></label><label>Leap-day birthday handling<select><option>February 28</option><option>March 1</option></select></label></div><div className="settings-actions"><button className="button button-primary" onClick={() => mutate({ action: "saveBudget", monthlyBudgetCents: Number(budget) * 100 })}>Save changes</button></div></section></div></>; }

function ModalShell({ title, description, close, children }: { title: string; description: string; close: () => void; children: React.ReactNode }) { return <div className="modal-backdrop" role="presentation" onMouseDown={(e) => { if (e.currentTarget === e.target) close(); }}><section className="modal" role="dialog" aria-modal="true" aria-label={title}><header><div><h2>{title}</h2><p>{description}</p></div><button onClick={close} aria-label="Close"><X /></button></header>{children}</section></div>; }

function AddEmployeeModal({ busy, close, submit }: { busy: boolean; close: () => void; submit: (payload: Record<string, unknown>) => void }) { function onSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); submit({ action: "addEmployee", ...Object.fromEntries(form.entries()), birthdayMonth: Number(form.get("birthdayMonth")), birthdayDay: Number(form.get("birthdayDay")) }); } return <ModalShell title="Add an employee" description="We'll start watching for their important moments." close={close}><form onSubmit={onSubmit}><div className="form-grid"><label>First name<input required name="firstName" autoFocus /></label><label>Last name<input required name="lastName" /></label></div><label>Work email<input required type="email" name="email" /></label><div className="form-grid"><label>Department<input required name="department" placeholder="e.g. Design" /></label><label>Job title<input required name="jobTitle" /></label></div><div className="form-grid three"><label>Birthday month<select name="birthdayMonth" defaultValue="8">{Array.from({ length: 12 }, (_, i) => <option value={i + 1} key={i}>{format(new Date(2020, i, 1), "MMMM")}</option>)}</select></label><label>Day<input name="birthdayDay" type="number" min="1" max="31" defaultValue="15" /></label><label>Hire date<input required name="hireDate" type="date" defaultValue={format(new Date(), "yyyy-MM-dd")} /></label></div><p className="privacy-note"><ShieldCheck /> Birth year is never required. Birthday data stays private to your organization.</p><footer><button type="button" className="button button-secondary" onClick={close}>Cancel</button><button className="button button-primary" disabled={busy}>{busy ? "Adding…" : "Add employee"}</button></footer></form></ModalShell>; }

function RecognitionModal({ employees, selected, setSelected, busy, close, submit }: { employees: Employee[]; selected: string; setSelected: (id: string) => void; busy: boolean; close: () => void; submit: (payload: Record<string, unknown>) => void }) { const [amount, setAmount] = useState(5000); function onSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); submit({ action: "recognize", employeeId: selected, recognitionType: form.get("recognitionType"), message: form.get("message"), amountCents: amount }); } return <ModalShell title="Celebrate Someone" description="Employee, reason, reward, message—done in under 30 seconds." close={close}><form onSubmit={onSubmit}><label>1. Employee<select value={selected} onChange={(e) => setSelected(e.target.value)}>{employees.map((employee) => <option value={employee.id} key={employee.id}>{fullName(employee)}</option>)}</select></label><label>2. Reason<select name="recognitionType"><option>Great Work</option><option>Above & Beyond</option><option>Customer Praise</option><option>Sales Goal</option><option>Project Complete</option><option>Team Player</option><option>Promotion</option><option>Custom</option></select></label><fieldset><legend>3. Reward</legend><div className="amount-picker">{[0,1000,2500,5000,7500,10000].map((value) => <button type="button" className={amount === value ? "active" : ""} onClick={() => setAmount(value)} key={value}>{value === 0 ? "Recognition only" : money(value)}</button>)}</div></fieldset><label>4. Message<textarea name="message" required defaultValue="Your work made a real difference. Thank you for going above and beyond!" rows={3} /></label><div className="confirmation-strip"><Gift /><span><b>{amount ? `${money(amount)} TEST digital reward` : "Recognition message"}</b><small>{amount ? "Scheduled through Tremendous Sandbox" : "No reward purchase"}</small></span></div><footer><button type="button" className="button button-secondary" onClick={close}>Cancel</button><button className="button button-primary" disabled={busy}>{busy ? "Sending…" : "5. Send Celebration"}</button></footer></form></ModalShell>; }

function QuickCelebrateModal({ data, selected, setSelected, busy, close, submit }: { data: Workspace; selected: string; setSelected: (id: string) => void; busy: boolean; close: () => void; submit: (payload: Record<string, unknown>) => void }) {
  const recommendation = data.recommendations.find((item) => item.employeeId === selected && item.status === "recommended") ?? data.recommendations.find((item) => item.employeeId === selected);
  const [amount, setAmount] = useState(recommendation?.amountCents ?? 2500);
  function onSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); submit({ action: "quickCelebrate", employeeId: selected, recommendationId: recommendation?.id, recognitionType: "Quick Celebrate", message: "You made a real difference. This celebration is for you!", amountCents: amount }); }
  return <ModalShell title="Quick Celebrate" description="A personalized celebration in under 30 seconds." close={close}><form onSubmit={onSubmit}><label>Employee<select value={selected} onChange={(event) => setSelected(event.target.value)}>{data.employees.map((employee) => <option value={employee.id} key={employee.id}>{fullName(employee)}</option>)}</select></label><section className="quick-recommendation"><span><WandSparkles /></span><div><small>PERKJOY RECOMMENDS</small><h3>{recommendation?.title ?? `${money(amount)} employee-choice reward`}</h3><p>{recommendation?.recommendationReason ?? "A flexible reward that works for any location."}</p>{recommendation?.somethingDifferent && <em>Something different this year</em>}</div><b>{money(amount)}</b></section><fieldset><legend>Change reward</legend><div className="amount-picker">{[0,2500,5000,7500,10000].map((value) => <button type="button" className={amount === value ? "active" : ""} onClick={() => setAmount(value)} key={value}>{value === 0 ? "Recognition only" : money(value)}</button>)}</div></fieldset><label>Message<textarea rows={3} defaultValue="You made a real difference. This celebration is for you!" /></label><p className="privacy-note"><ShieldCheck /> The recommendation reason is private and is never included in the employee message.</p><footer><button type="button" className="button button-secondary" onClick={close}>Cancel</button><button className="button button-primary" disabled={busy}>{busy ? "Scheduling…" : amount ? `Send ${money(amount)} reward` : "Send recognition"}</button></footer></form></ModalShell>;
}

function ConciergeModal({ employees, selected, setSelected, busy, close, submit }: { employees: Employee[]; selected: string; setSelected: (id: string) => void; busy: boolean; close: () => void; submit: (payload: Record<string, unknown>) => void }) {
  const [budget, setBudget] = useState(100);
  const deliveryDate = format(new Date(Date.now() + 7 * 86400000), "yyyy-MM-dd");
  function onSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); submit({ action: "createConcierge", employeeId: selected, occasion: form.get("occasion"), budgetCents: budget * 100, deliveryDate: form.get("deliveryDate") }); }
  return <ModalShell title="PerkJoy Concierge" description="Tell us the occasion and budget. We'll handle the rest." close={close}><form onSubmit={onSubmit}><label>Employee<select value={selected} onChange={(event) => setSelected(event.target.value)}>{employees.map((employee) => <option value={employee.id} key={employee.id}>{fullName(employee)}</option>)}</select></label><div className="form-grid"><label>Occasion<select name="occasion"><option>Birthday</option><option>Work Anniversary</option><option>Team Achievement</option><option>New Hire</option><option>Custom Celebration</option></select></label><label>Delivery date<input name="deliveryDate" type="date" min={deliveryDate} defaultValue={deliveryDate} /></label></div><label>Celebration budget<div className="money-input"><span>$</span><input type="number" min="25" max="2500" value={budget} onChange={(event) => setBudget(Number(event.target.value))} /></div></label><section className="concierge-process"><span><b>1</b> Preferences reviewed</span><span><b>2</b> Celebration planned</span><span><b>3</b> You approve</span><span><b>4</b> PerkJoy coordinates delivery</span></section><p className="privacy-note"><ShieldCheck /> Nothing is purchased until an authorized company admin approves the recommendation.</p><footer><button type="button" className="button button-secondary" onClick={close}>Cancel</button><button className="button button-primary" disabled={busy}>{busy ? "Submitting…" : "Send to Concierge"}</button></footer></form></ModalShell>;
}

function OrderModal({ employees, product, selected, setSelected, busy, close, submit }: { employees: Employee[]; product: Product; selected: string; setSelected: (id: string) => void; busy: boolean; close: () => void; submit: (payload: Record<string, unknown>) => void }) { const tomorrow = format(new Date(Date.now() + 86400000 * 3), "yyyy-MM-dd"); function onSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); submit({ action: "createOrder", employeeId: selected, productId: product.id, deliveryDate: form.get("deliveryDate") }); } return <ModalShell title="Send a local gift" description="PerkJoy will confirm availability and coordinate delivery." close={close}><form onSubmit={onSubmit}><div className="selected-product"><span>🎂</span><div><small>{product.vendorName}</small><b>{product.name}</b><p>Serves {product.servesPeople}</p></div><b>{money(product.priceCents)}</b></div><label>Employee<select value={selected} onChange={(e) => setSelected(e.target.value)}>{employees.map((employee) => <option value={employee.id} key={employee.id}>{fullName(employee)}</option>)}</select></label><label>Delivery date<input required name="deliveryDate" type="date" min={tomorrow} defaultValue={tomorrow} /></label><label>Gift message<textarea rows={3} defaultValue="Hope your day is as wonderful as you are. Happy Birthday!" /></label><div className="order-total"><span>Product <b>{money(product.priceCents)}</b></span><span>Delivery <b>{money(product.deliveryFeeCents)}</b></span><hr /><span>Total <b>{money(product.priceCents + product.deliveryFeeCents)}</b></span></div><p className="privacy-note"><ShieldCheck /> Availability and minimum notice are checked before the order enters the fulfillment queue.</p><footer><button type="button" className="button button-secondary" onClick={close}>Cancel</button><button className="button button-primary" disabled={busy}>{busy ? "Placing order…" : `Confirm ${money(product.priceCents + product.deliveryFeeCents)}`}</button></footer></form></ModalShell>; }

function AppSkeleton() { return <div className="app-page"><aside className="app-sidebar"><div className="sidebar-head"><Logo /></div></aside><section className="app-workspace"><header className="app-topbar" /><main className="app-content"><div className="skeleton title-skeleton" /><div className="kpi-grid">{Array.from({ length: 4 }, (_, i) => <div className="skeleton kpi-skeleton" key={i} />)}</div><div className="skeleton table-skeleton" /></main></section></div>; }
